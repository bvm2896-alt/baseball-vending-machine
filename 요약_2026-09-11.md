# 야구자판기 — 오늘(9/10 밤~9/11 새벽) 한 일 + 전체 프로세스 요약

## 1. 오늘 집에 와서 한 것

**환경 옮기기**
- 회사 야구자판기 폴더를 통째로 집 PC(`C:\Users\sh705\OneDrive\바탕 화면\야구자판기`)로 복사. 설정·토큰·폰트·.git 이 다 같이 와서 pc연결은 필요 없었음.
- 결과물(영상·썸네일) 저장 위치를 구글 드라이브 `H:\내 드라이브\야구자판기_영상확인` 으로 변경(설정.txt `OUTPUT_DIR`). 폰에서 바로 확인 가능.
- 집 PC에 Claude Code CLI, Agent Reach(유튜브 자막·깃허브·RSS), /watch(유튜브 영상 분석), skill-creator, 제미나이 교차검증 세팅 완료(별도 채팅). 이걸 야구자판기에 붙이기 위해 `시스템\CLAUDE.md`(작업 규칙서)를 만들어 넣음.

**영상 품질**
- 화질: 1080p 30fps → **2K(1440x2560) 60fps** 렌더로 교체(frames.js 병렬 렌더). 4K·120fps는 유튜브가 60fps 상한이라 60 유지, 4K는 설정 `VIDEO_SCALE=2` 로 가능.
- 템플릿 전체 밝기 올림(배경·카드·보조색·글로우).
- 날짜 통일: 썸네일 배지 "9월 9일 (수)" 박스형 132px, 영상 우상단 날짜·저장 폴더·나레이션 "N월 N일 기준" 전부 **경기 날짜** 기준.
- 자막: 음성 파일 시작이 아니라 **실제 말 시작~끝**에만 표시. 0.45초 미만 짧은 조각은 합침("첫째" 빨리 넘어감, "그래도 제 예측은 기아에요" 깜빡임 해결).

**음성**
- 편별 음성 폴더 분리(1편 영상에 2편 음성 붙던 버그 해결).
- 잘린 음성은 렌더 중 자동 재합성. 타입캐스트 예비 계정 자동 전환.

**밤 제작 방식 검토**
- 아침 대신 경기 끝난 밤에 만드는 게 가능한지 보려고 9/10 경기 재료 탐색 → 콘티 2편 작성 → 피드백(구체성 부족, 무관한 팩트, 흐름) 반영해 재작성.
- run_daily에 **밤 모드** 추가: 18시 이후 실행하면 다음날 콘티(내일 날짜 파일)로 제작, 경기 날짜는 당일.
- 일정·예고 선발투수 수집기(fetch_schedule.js) 추가 → 콘티에 "11일 삼성 페덱 vs 키움", "KT 로건 vs 롯데" 같은 다음 경기 정보 포함. 선발 파싱 오류는 새벽에 수정 완료.

**지금 상태**
- 9/11 콘티 2편(1편 삼성 vs KT 1위 예측, 2편 LG vs 기아 3위 예측)이 `시스템\episodes\` 에 들어가 있음.
- 00:33 실행한 지금실행이 00:40쯤 멈춰서 아직 영상은 안 만들어짐 → **지금실행 한 번 누르면 바로 제작**(콘티가 있으니 대기 없음, 약 10분).
- 회사 PC는 아직 옛 버전. 집에서 지금실행/깃저장을 누르면 깃허브에 올라가고, 회사에서 지금실행 누르면 자동으로 받음.

## 2. 전체 프로세스 (어떻게 돌아가나)

**한 줄 요약**: PC가 순위·뉴스·일정을 긁어오면 → Claude가 그걸 보고 콘티 2편을 써서 PC 폴더에 넣고 → PC가 음성 만들고 검수하고 영상 렌더해서 구글 드라이브에 저장 → 확인 후 신호 주면 유튜브 업로드.

**단계별**
1. `지금실행.cmd` (또는 07:30 스케줄) → 깃허브와 동기화(이 PC 변경분 올리고 최신 받음)
2. 재료 수집: 네이버 순위(fetch_rank) + 구글 뉴스 최근 1일(fetch_news) + 네이버 일정·예고 선발(fetch_schedule) → `시스템\data\`
3. 콘티 대기(최대 45분): Claude가 재료 보고 `episodes\날짜_1.json, _2.json` 작성 → PC에 넣음. 콘티가 이미 있으면 바로 진행.
4. `build.py prep`: 숫자 읽기(고유어/한자어)·자막 길이 등 규칙 검사
5. `tts.py`: 타입캐스트로 줄마다 한 번에 합성(억양 유지), 쉼 자리에 0.18초만 삽입
6. `qa_voice.py`: 톤·속도·앞뒤 깨짐·긴 무음 검사 → 이상한 줄은 재합성, whisper로 자막 타이밍 정밀 맞춤
7. `build.py render` + `frames.js`: 2K 60fps 렌더, 썸네일, 유튜브 제목·설명 파일 생성
8. 저장: `H:\내 드라이브\야구자판기_영상확인\경기날짜\번호_팀.mp4 + _썸네일.jpg + _유튜브.txt`
9. 신호 감시: `업로드.txt` → 비공개 업로드, `공개.txt` → 공개 전환, `재생성.txt`, `종료.txt`/`절전.txt` → PC 끄기. 진행 상황은 `야구자판기\오늘.txt`.

**버튼 정리**
- 지금실행: 전체 실행(깃 동기화 포함)
- 영상만다시: 음성 재사용, 영상만 다시(크레딧 안 씀)
- 깃저장: 영상 안 만들고 깃허브 동기화만(PC 옮기기 전)
- 일정확인: 오늘·내일 일정·선발만 수집
- 작업등록: 07:30 자동 실행 등록(최초 1회)

**두 PC 규칙**
- 깃허브 저장소가 원본. 떠나는 PC에서 깃저장 → 도착 PC에서 지금실행.
- Claude(이 채팅)는 깃허브에 직접 못 올림 → PC 폴더에 파일을 넣어 두면 PC가 올림.
- 설정.txt·client_secret·token·work·data·영상은 각 PC에만(깃허브 X).

**콘티 규칙 핵심**
- 그날 가장 뜨거운 주제 2편, 결 안 겹치게. 예측형은 변수(남은 경기·맞대결 수와 장소·상대전적·다음 상대와 선발·AG 차출·부상 복귀)를 여러 개 놓고 결론. "1위 누가 할지 경우의 수와 변수 볼게요"로 시작.
- 나레이션: 팀명 한글 발음, 숫자 읽기 규칙, ~인데요/~에요, 콤마·마침표 X, 호흡 단위 ` / `.
- 자막 2줄·16자, 마지막 질문은 양자택일, 밤 제작이면 "어제" 대신 "10일 경기".
- 아직 업로드한 영상이 없으니 이전 영상 언급 X. 화요일은 순위 리뷰 대신 다른 주제(예: 9/21 신인드래프트 편 초안 있음).

## 3. 다음에 할 일
- 지금실행 눌러 9/11 영상 2편 만들고 확인 → 아침 제작 vs 밤 제작 결정
- 집 PC 시스템 폴더에서 `claude` 실행 → 렌더 결과 검수 한 번 해보기(Claude Code 접목 1단계)
- 회사 PC 갱신(집에서 깃저장 후 회사에서 지금실행)
- 첫 업로드(최종본만 한 번), Cowork 예약 콘티 작업, 배경색(팀 색 vs 고정) 결정
